# agents/agent01.yaml
# Slot generico para taller. Reasignar `name`, `owner` y `judgment_rules`
# al participante cuando se haga la asignacion.

id: agent01
extends: profiles/base.yaml

identity:
  name: "Agent 01"                                # TODO: reemplazar al asignar
  owner: "unassigned@workshop.local"              # TODO: email del participante
  # locale, tone, judgment_rules → heredan del base.yaml hasta asignar.

channels:
  telegram:
    enabled: true
    bot_token: "${TELEGRAM_BOT_TOKEN_AGENT01}"
    chat_id: "${TELEGRAM_CHAT_ID_AGENT01}"

policies:
  private_domains: []

deployment:
  coolify_app_id: "openclaw-agent01"
